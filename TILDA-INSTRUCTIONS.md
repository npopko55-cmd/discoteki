# Инструкция для спеца — вставка в Tilda

**Страница:** walk-walk.ru/discoteki

## Что вставлять

**3 блока T123** (HTML-код) на странице, ПОДРЯД друг за другом, в правильном порядке:

| Порядок | Файл | Размер | Что внутри |
|---------|------|--------|-----------|
| 1 | `tilda-block-1.html` | 16 KB | Подключение шрифтов/CSS + шапка + Hero + «Уникальный формат» + «Наполнение» (7 вечеринок + бонусы) |
| 2 | `tilda-block-2.html` | 26 KB | Тренеры + Тарифы + Кейсы + Финальный CTA + Footer |
| 3 | `tilda-block-3.html` | 4 KB | Попапы (видео Kinescope + GetCourse-виджеты оплаты) + sticky-кнопка + back-to-top + подключение JS |

Все три файла ≤ 26 KB (лимит Tilda 30 000 символов).

## Важно

1. **3 блока идут ПОДРЯД** на странице, между ними ничего не вставлять (иначе попапы/sticky-кнопка могут оказаться в неправильном контейнере).
2. **Вся кириллица закодирована в HTML-сущности** (`&#NNNN;`) — это лечит mojibake (когда русский текст превращается в «РёР°РіР°Р№»). Не пугаться, что в коде «непонятные символы» — браузер собирает обратно русский.
3. **CSS, JS и картинки** грузятся с GitHub Pages: `https://npopko55-cmd.github.io/discoteki/`. В Tilda ничего загружать не нужно.
4. Cache-busting `?v=disco-AB` — при правках версия меняется автоматически, повторно вставлять блоки не нужно.

## Видео тренеров (Kinescope)
В блоке 3 — 3 попапа с Kinescope-iframe (Настя / Дима / Маша). Видео грузится лениво при открытии попапа, при закрытии останавливается.

## Виджеты оплаты (GetCourse)
В блоке 3 — 4 попапа с GetCourse-виджетами:
- Только дискотеки 990 ₽ → id 1618358
- + клуб 2 мес 3 490 ₽ → id 1618352
- + клуб 6 мес 4 490 ₽ → id 1618353
- + клуб 2 года 8 990 ₽ → id 1618354

## Дополнительно в настройках Tilda
1. **Метрика 94057307** — подключить на странице (Настройки сайта → Аналитика → Yandex.Metrika). Цели полетят автоматически через `data-metrika-goal`.
2. **UTM-метки** — пробрасываются автоматически JS на все ссылки walk-walk.ru. GetCourse-виджет дополнительно сам читает UTM из URL страницы.

## ⚠️ Что проверить после вставки
Открыть `walk-walk.ru/discoteki?utm_source=test&utm_medium=test`, кликнуть тариф → в GetCourse-заказе должны быть метки `test`.

## Если нужно обновить
```bash
cd discoteki
python3 build-tilda-split.py
```
Три файла обновятся — заново скопировать в Tilda T123 блоки.
